/**
 * Test for RMSDetector.
 * Runs with mocha.
 */
'use strict'

const RMSDetector = require('../lib/helpers/RMSDetector')
const { ok, equal } = require('assert')

describe('r-m-s-detector', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
